# rxdb-schema

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rxdb-schema` to build the library.

## Running unit tests

Run `nx test rxdb-schema` to execute the unit tests via [Jest](https://jestjs.io).
